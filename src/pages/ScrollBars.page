<apex:page controller="ScrollBarsCls" sidebar="false">
  <apex:pageBlock title="Accounts">
      <apex:outputpanel style="overflow:scroll;width:1280px;height:150px;" layout="block">
      <apex:pageBlockTable value="{!accs}" var="a">
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
          <apex:column value="{!a.id}"/>
          <apex:column value="{!a.Name}"/>
          <apex:column value="{!a.Phone}"/>
      </apex:pageBlockTable>
      </apex:outputPanel>
  </apex:pageBlock>
</apex:page>