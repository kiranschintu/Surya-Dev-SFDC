public class MathClass{
    public static void calc(){
    	DECIMAL a=15;
        DECIMAL b=6;
        Decimal Result =a/b;
        Integer aa=13;
        Integer bb=2;
        
        INTEGER RESULT1 = INTEGER.VALUEOF(result.setScale(0, RoundingMode.CEILING ));
        
        system.debug('Result : '+Result1);
        system.debug('Result1 : '+aa/bb);
    }
}